### Usage
Let the person using the package define his driver and browser paths

Since the package will be installed on her system and he has to install the driver and browser at his ends as well

We can include a tutorial for installing the necessary driver and browsers


### browser_configs
We have the browser and driver paths predefined in browser configs

The verifypaths functions ensures that the browser and driver paths adn files exits in the specified paths